/**
 * Auth services barrel export
 *
 * @description Centralizes exports for authentication API services.
 * Services handle all API communication for the auth feature.
 *
 * @example
 * ```typescript
 * import { authService, organizationService } from '@/features/auth/services';
 * ```
 */

export { authService } from './auth.service';
export { organizationService } from './organization.service';
