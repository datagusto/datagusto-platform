/**
 * Shared hooks barrel export
 *
 * @description Centralizes exports for custom hooks used across multiple features.
 * These hooks provide reusable logic for common patterns.
 *
 * @example
 * ```typescript
 * import { useApi, useDebounce } from '@/shared/hooks';
 * ```
 */

// Export shared hooks here
// export { useApi } from './use-api';
// export { useDebounce } from './use-debounce';
