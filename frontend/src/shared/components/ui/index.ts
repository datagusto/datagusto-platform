/**
 * UI components barrel export
 *
 * @description Centralizes exports for shadcn/ui and Radix UI components.
 * These are reusable, accessible UI primitives used across the application.
 *
 * @example
 * ```typescript
 * import { Button, Input, Dialog } from '@/shared/components/ui';
 * ```
 */

// Export UI components here (shadcn/ui)
// export { Button } from './button';
// export { Input } from './input';
// export { Dialog } from './dialog';
