/**
 * Common components barrel export
 *
 * @description Centralizes exports for common UI components used across features.
 * These are generic components like loading spinners, error messages, etc.
 *
 * @example
 * ```typescript
 * import { Loading, ErrorMessage } from '@/shared/components/common';
 * ```
 */

export { Loading } from './loading';
export { ErrorMessage } from './error-message';
