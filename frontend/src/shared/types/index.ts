/**
 * Shared types barrel export
 *
 * @description Centralizes exports for TypeScript types used across multiple features.
 * Includes common types, utility types, and shared interfaces.
 *
 * @example
 * ```typescript
 * import type { ApiResponse, PaginatedResponse } from '@/shared/types';
 * ```
 */

// Export shared types here
// export type { ApiResponse, ApiError } from './api.types';
// export type { PaginatedResponse } from './pagination.types';
