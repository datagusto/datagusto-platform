/**
 * Configuration barrel export
 *
 * @description Centralizes exports for application configuration.
 * Includes API endpoints, route definitions, and global constants.
 *
 * @example
 * ```typescript
 * import { API_CONFIG, API_ENDPOINTS, HTTP_STATUS } from '@/shared/config';
 * ```
 */

export { API_CONFIG, API_ENDPOINTS, HTTP_STATUS } from './api.config';

// Export routes configuration (future)
// export { ROUTES } from './routes.config';

// Export constants (future)
// export * from './constants';
